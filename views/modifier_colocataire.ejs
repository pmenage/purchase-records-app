<!DOCTYPE html>
<html>
    <head>
        <title>Modification du colocataire <%= rows_colocataire[0].id_colocataire %></title>
        <meta charset="utf-8" />
        <link rel="stylesheet" type="text/css" href="https://fonts.googleapis.com/css?family=Roboto:300,400,700" />
        <link rel="stylesheet" type="text/css" href="https://fonts.googleapis.com/css?family=Roboto+Slab:300" />
        <link rel="stylesheet" type="text/css" href="../bootstrap-3.3.7-dist/css/bootstrap.min.css" />
        <link rel="stylesheet" type="text/css" href="../bootstrap-3.3.7-dist/css/bootstrap-theme.min.css" />
        <link rel="stylesheet" type="text/css" href="../bootstrap-3.3.7-dist/js/bootstrap.min.js" />
        <style>
            body {
                font-family: 'Roboto';
            }
            
            h2 h3 {
                font-family: 'Roboto Slab', serif;
            }
        </style>
    </head>

    <body>
        
        <h2>Modification</h2>
        <div>Remplacez les champs à modifier par la valeur souhaitée.</div>
        <form action="/modifier_colocataire_2" method="post"><br>
            <input type="hidden" name="colocataire" value="<%= colocataire %>">
            Personne : 
            <select name="personne">
                <% for (var i = 0; i < rows_personne.length; i++) {
                    if (rows_personne[i].ID_PERSONNE == rows_colocataire[0].ID_PERSONNE) { %>
                        <option value="<%= rows_personne[i].ID_PERSONNE %>" selected><%= rows_personne[i].PRENOM_PERSONNE + ' ' + rows_personne[i].NOM_PERSONNE %></option>
                    <% } else { %>
                        <option value="<%= rows_personne[i].ID_PERSONNE %>"><%= rows_personne[i].PRENOM_PERSONNE + ' ' + rows_personne[i].NOM_PERSONNE %></option>
                    <% }
                } %>
            </select><br>
            Colocation : 
            <select name="colocation">
                <% for (var i = 0; i < rows_colocation.length; i++) {
                    if (rows_colocation[i].ID_COLOCATION == rows_colocataire[0].ID_COLOCATION) { %>
                        <option value="<%= rows_colocation[i].ID_COLOCATION %>" selected><%= rows_colocation[i].NOM_COLOCATION %></option>
                    <% } else { %>
                        <option value="<%= rows_colocation[i].ID_COLOCATION %>"><%= rows_colocation[i].NOM_COLOCATION %></option>
                    <% }
                } %>
            </select><br>
            Date d'entrée : <input type="text" name="date_entree" value="<%= rows_colocataire[0].DATE_ENTREE_COLOC %>"><br>
            <% if (rows_colocataire[0].DATE_SORTIE_COLOC == null) { %>
                Date de sortie : <input type="text" name="date_sortie" value="null"><br>
            <% } else { %>
                Date de sortie : <input type="text" name="date_sortie" value="<%= rows_colocataire[0].DATE_SORTIE_COLOC %>"><br>
            <% } %>
            <button type="submit">Modifier</button>
        </form>
        
    </body>
</html>